services:
  viewer:
    image: ghcr.io/nuxper/geocalib-viewer:latest
    network_mode: host
    environment:
      - PORT=6001
    volumes:
      - ${IMAGES_DIR:-/data}:/data:ro
    restart: unless-stopped
